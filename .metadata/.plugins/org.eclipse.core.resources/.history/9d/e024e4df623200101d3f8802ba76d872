spring:
  application:
    name: patientManagement

  datasource:
    url: jdbc:h2:mem:dbPatient
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    open-in-view: true
  cloud:
   discovery:
	   enabled: false
server:
  port: 5000
